import React, { PureComponent } from 'react';

import { ns } from '../helpers/xuiClassNamespace';

export default class XUIEditableTable extends PureComponent {
  render() {
    return <div className={`${ns}-editabletable`}>XUIEditableTable</div>;
  }
}
