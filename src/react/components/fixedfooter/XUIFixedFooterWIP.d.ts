import * as React from 'react';

interface Props {
  children?: React.ReactNode;
  className?: string;
  qaHook?: string;
}

export default class XUIFixedFooterWIP extends React.PureComponent<Props> {}
