import { ns } from '../../helpers/xuiClassNamespace';

export const baseClass = `${ns}-illustration`;

export const sizeClasses = {
  small: `${baseClass}-small`,
  medium: `${baseClass}-medium`,
  large: `${baseClass}-large`,
};
