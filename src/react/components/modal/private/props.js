import PropTypes from 'prop-types';
export const propTypes = {
	/**
	 * @property {boolean} [defaultLayout=true] If the modal will use the default XUI style layout
	 */
	defaultLayout: PropTypes.bool
};

export const defaultProps = {
	defaultLayout: true
};
