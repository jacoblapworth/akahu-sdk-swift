const rowVariants = {
  flex: 'flex',
  grid: 'grid',
  standard: '',
};

const columnShortNames = {
  full: 12,
  half: 6,
  third: 4,
  quarter: 3,
};

const overviewSentiments = {
  standard: '',
  positive: 'positive',
  negative: 'negative',
  muted: 'muted',
};

module.exports = {
  rowVariants,
  columnShortNames,
  overviewSentiments,
};
