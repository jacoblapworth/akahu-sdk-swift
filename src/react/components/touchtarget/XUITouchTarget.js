import React from 'react';

import { ns } from '../helpers/xuiClassNamespace';

const baseClass = `${ns}-touchtarget`;

const XUITouchTarget = () => <div className={baseClass} />;

export default XUITouchTarget;
